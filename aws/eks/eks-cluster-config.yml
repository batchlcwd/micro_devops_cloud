apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig


metadata:
  name: lcwd-cluster1
  region: ap-south-1
  version: "1.36"

iam:
  withOIDC: true


managedNodeGroups:
  - name: lcwd-cluster1
    instanceType: t3.large
    minSize: 1
    maxSize: 4
    desiredCapacity: 2
    volumeSize: 30
    volumeType: gp3
    privateNetworking: false
    iam:
      withAddonPolicies:
        ebs: true
        albIngress: true
        cloudWatch: true